'use client'; import {useAccount,useConnect,useDisconnect} from 'wagmi'; import Link from 'next/link';
export default function Wallet(){const {address,isConnected}=useAccount();const {connect,connectors}=useConnect();const {disconnect}=useDisconnect();return <main className="shell"><nav className="nav"><Link className="brand" href="/">ves<span>to</span></Link><Link className="btn" href="/discover">Discover</Link></nav><section className="hero"><div className="eyebrow">Base wallet</div><h1>Your positions stay yours.</h1><p>Connect an injected wallet on Base. Vesto never asks for private keys or custody of assets.</p>{isConnected?<><div className="card"><b>Connected</b><p className="muted">{address}</p><p className="green">Base mainnet · chain 8453</p></div><div className="actions"><Link className="btn primary" href="/portfolio">Open portfolio</Link><button className="btn" onClick={()=>disconnect()}>Disconnect</button></div></>:<div className="actions">{connectors.map(c=><button className="btn primary" key={c.uid} onClick={()=>connect({connector:c})}>Connect {c.name}</button>)}</div>}</section></main>}
