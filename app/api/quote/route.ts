import {NextRequest,NextResponse} from 'next/server';
export async function GET(req:NextRequest){const p=req.nextUrl.searchParams;const sellToken=p.get('sellToken');const buyToken=p.get('buyToken');const sellAmount=p.get('sellAmount');const taker=p.get('taker');if(!sellToken||!buyToken||!sellAmount||!taker)return NextResponse.json({error:'Missing quote parameters'},{status:400});const key=process.env.ZEROX_API_KEY;if(!key)return NextResponse.json({error:'Trading is not configured. Add ZEROX_API_KEY to the deployment environment.'},{status:503});const url=new URL('https://api.0x.org/swap/allowance-holder/quote');url.searchParams.set('chainId','8453');url.searchParams.set('sellToken',sellToken);url.searchParams.set('buyToken',buyToken);url.searchParams.set('sellAmount',sellAmount);url.searchParams.set('taker',taker);const r=await fetch(url,{headers:{'0x-api-key':key,'0x-version':'2'},cache:'no-store'});return NextResponse.json(await r.json(),{status:r.status});}
