import Link from 'next/link';
import { theses, assets } from '../lib/assets';
export default function Home(){return <main className="shell"><nav className="nav"><div className="brand">ves<span>to</span></div><div className="navlinks"><Link href="/discover">Discover</Link><Link href="/feed">Feed</Link><Link href="/portfolio">Portfolio</Link></div><Link className="btn" href="/wallet">Connect wallet</Link></nav><section className="hero"><div className="eyebrow">Ideas → positions</div><h1>See what people believe. Build the portfolio.</h1><p>Vesto is the social investing layer for tokenized assets on Base. Discover a thesis, understand the allocation, remix it, and take eligible assets onchain.</p><div className="actions"><Link className="btn primary" href="/discover">Explore ideas</Link><Link className="btn" href="/theses/new">Create a thesis</Link></div></section><section className="section"><div className="sectionhead"><h2>Ideas people are building around</h2><Link className="muted" href="/discover">View all →</Link></div><div className="grid">{theses.map(t=><Link className="card" href={'/theses/'+t.id} key={t.id}><div className="idea"><div><span className="tag">{t.tags[0]}</span><h3>{t.title}</h3><p className="muted">{t.summary}</p><small className="muted">{t.creator} · 2.4k followers</small></div><div className="weight">{t.assets.length}</div></div></Link>)}</div></section><section className="section"><div className="sectionhead"><h2>Tokenized stocks on Base</h2><span className="muted">Verified registry</span></div><div className="card">{assets.map(a=><div className="assetrow" key={a.symbol}><div><b>{a.symbol}</b><span className="muted"> · {a.name}</span></div><span className="tag">{a.theme}</span></div>)}</div></section></main>}
