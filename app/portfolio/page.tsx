import Link from 'next/link'; import {theses,assets} from '../../lib/assets'; import {InvestPanel} from '../../components/InvestPanel';
export default function Portfolio(){const t=theses[0];return <main className="shell"><nav className="nav"><Link className="brand" href="/">ves<span>to</span></Link><div className="navlinks"><Link href="/discover">Discover</Link><Link href="/feed">Feed</Link><Link href="/portfolio">Portfolio</Link></div><Link className="btn" href="/wallet">Wallet</Link></nav><section className="section"><div className="eyebrow">Portfolio builder</div><h1>{t.title}</h1><p className="muted">Remix the model, then execute only what you choose.</p></section><div className="feed"><section className="card"><h2>Target allocation</h2>{t.assets.map(([symbol,weight])=>{const a=assets.find(x=>x.symbol===symbol);return <div className="assetrow" key={symbol}><div><b>{symbol}</b><div className="muted">{a?.name}</div></div><strong>{weight}%</strong></div>})}<div className="actions"><button className="btn">Remix allocation</button><button className="btn">Save version</button></div></section><InvestPanel token={assets[0].address as `0x${string}`} symbol={assets[0].symbol}/></div><section className="section"><div className="card"><b>Execution guardrails</b><p className="muted">Vesto never holds your keys. Every trade is quoted before signing and submitted from your connected wallet. Tokenized stocks are offered only in eligible jurisdictions; review issuer disclosures before trading.</p></div></section></main>}
