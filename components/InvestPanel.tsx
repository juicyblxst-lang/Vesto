'use client';
import {useState} from 'react'; import {useAccount,useSendTransaction,useWriteContract} from 'wagmi'; import {parseUnits} from 'viem'; import {BASE_USDC,erc20Abi,explorer} from '../lib/constants';
export function InvestPanel({token,symbol}:{token:`0x${string}`;symbol:string}){const {address}=useAccount();const {writeContractAsync}=useWriteContract();const {sendTransactionAsync}=useSendTransaction();const [amount,setAmount]=useState('25');const [status,setStatus]=useState('');const [hash,setHash]=useState('');const invest=async()=>{if(!address){setStatus('Connect your Base wallet first.');return}try{setStatus('Getting executable quote…');const q=await fetch(`/api/quote?sellToken=${BASE_USDC}&buyToken=${token}&sellAmount=${parseUnits(amount,6).toString()}&taker=${address}`).then(r=>r.json());if(!q.transaction||!q.issues?.allowance?.spender){if(q.error){setStatus(q.error);return}throw new Error('No executable quote returned.')}setStatus('Approving USDC…');await writeContractAsync({address:BASE_USDC,abi:erc20Abi,functionName:'approve',args:[q.issues.allowance.spender,q.issues.allowance.amount]});setStatus('Submitting swap…');const tx=await sendTransactionAsync({to:q.transaction.to,data:q.transaction.data,value:BigInt(q.transaction.value||'0')});setHash(tx);setStatus('Submitted. Waiting for Base confirmation in your wallet.');}catch(e){setStatus(e instanceof Error?e.message:'Transaction failed.')}};return <div className="card"><h3>Invest in {symbol}</h3><p className="muted">Real USDC → tokenized-stock swap on Base when an executable route is available.</p><input className="btn" style={{width:'100%',marginBottom:10}} value={amount} onChange={e=>setAmount(e.target.value.replace(/[^0-9.]/g,''))} inputMode="decimal"/><button className="btn primary" style={{width:'100%'}} onClick={invest}>Invest ${amount}</button>{status&&<p className="muted">{status}</p>}{hash&&<a className="green" href={explorer(hash)} target="_blank">View transaction ↗</a>}</div>}
